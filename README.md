# 连点器 (AndroidAC)

一个简洁、**完全离线**的 Android 连点器（自动点击）工具。基于无障碍服务（`dispatchGesture`）在你指定的屏幕位置模拟点击，配合悬浮控制球随时开始/停止。

> 本应用不读取屏幕内容、不联网、不收集任何数据。

## 功能特性

- **指定坐标连点**：屏幕上以十字标记定位点击位置，可拖动调整。
- **悬浮控制球**：单击开始/停止，长按进入「拖动定位模式」。
- **可调点击间隔**：自定义间隔毫秒数，数值越小点击越快。
- **点击穿透**：锁定后点击落到下层应用，不会误触标记本身。
- **轻量常驻**：悬浮窗由无障碍服务上下文创建，无需额外前台服务保活。

## 安装

1. 在 [Releases](../../releases) 页面下载最新的 APK。
2. 在手机上允许「安装未知来源应用」后安装。
3. 首次启动按引导开启两项权限：
   - **① 悬浮窗权限**（`SYSTEM_ALERT_WINDOW`）
   - **② 无障碍服务**

> ⚠️ 侧载（非应用商店）安装时，Android 13+ 开启无障碍前可能被「受限设置」拦截。
> 请到 **设置 → 应用 → 连点器 → 右上角菜单 → 允许受限设置**，再回到无障碍页面开启本服务。

## 使用方法

开启权限后，屏幕中央会出现十字标记与悬浮控制球：

- **单击控制球**：开始 / 停止连点
- **长按控制球**：进入 / 退出「拖动定位模式」，可拖动十字到目标位置
- 锁定后点击会穿透标记落到下层应用

## 构建

Android 项目，使用 Gradle (Kotlin DSL)：

| 项目 | 值 |
| --- | --- |
| `applicationId` | `com.casual.autoclicker` |
| `minSdk` | 24 |
| `targetSdk` | 32 |
| `compileSdk` | 33 |

```bash
./gradlew assembleDebug      # 生成 debug APK
./gradlew assembleRelease    # 生成 release APK（需配置签名）
```

## 免责声明

本工具仅供学习与个人自动化使用。请遵守相关应用的使用条款及当地法律法规，因不当使用造成的后果由使用者自行承担。

## 许可证

[MIT](LICENSE)
